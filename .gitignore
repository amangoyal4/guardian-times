node_modules/
*.log
.DS_Store
preview/
live.tmp.html
# NOTE: public/briefing.mp3 is intentionally COMMITTED (not ignored) so that a
# backup/"skip" run can re-deploy today's edition WITH its audio if the morning
# deploy hit a transient GitHub Pages error. Costs ~3MB/day of history — the price
# of a self-healing deploy.
voice-samples/
